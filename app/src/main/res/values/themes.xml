<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Common application theme for both day and night modes. -->
    <style name="Base.Theme.FireRunner" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your common theme here. -->
        <item name="android:windowBackground">@color/my_default_background_color</item>
    </style>

    <style name="Theme.FireRunner_Day" parent="Base.Theme.FireRunner" />
</resources>
